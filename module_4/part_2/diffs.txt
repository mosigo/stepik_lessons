1. В сгенерированном коде тест регистрируется с константным email-ом, то есть в следующий раз с теми же данными
тест уже не будет проходить.

2. В сгенерированном коде нет ожиданий элементов, то есть существует риск случайного падения теста.

3. В сгенерированном коде совершаются "лишние" действия, например, устанавливается размер окна или выполняется
клик в поле ввода, прежде чем в него вводится значение.

4. Сгенерированный код не использует фикстуры, то есть в каждом тесте будет отдельно прописано, как создаётся объект
браузера и как удаляется.

5. В сгенерированном коде нет разделения на arrange / act / assert.

6. В сгенерированном коде все локаторы определены прямо на месте, без вынесения их в константы.